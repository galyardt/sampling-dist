You can run the sampling distribution app from the R command line. 

1. Make sure you have the shiny package installed. 
2. Enter the commands: 
     library(shiny)
     runGitHub( "sampling-dist", "galyardt") 

